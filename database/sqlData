CREATE DATABASE BTLoop
ON
(
    NAME = BTLoop_data,
    FILENAME = 'C:\BTLoop_data.mdf',
    SIZE = 10MB,
    MAXSIZE = 100MB,
    FILEGROWTH = 5MB
)
LOG ON
(
    NAME = BTLoop_log,
    FILENAME = 'C:\BTLoop_log.ldf',
    SIZE = 10MB,
    MAXSIZE = 100MB,
    FILEGROWTH = 5MB
);
